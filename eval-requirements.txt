# Install alongside backend-requirements.txt:
#   pip install -r backend-requirements.txt -r eval-requirements.txt
#
# ragas>=0.4 hard-imports a class (ChatVertexAI) that was removed from
# langchain-community>=0.4 -- a known, currently-open upstream bug, not
# specific to this project: https://github.com/vibrantlabsai/ragas/issues/2745
# Pinning ragas==0.3.9 here avoids it. backend-requirements.txt separately
# pins langchain-community<0.4 for the same reason -- both pins together are
# the combination actually verified to work.
ragas==0.3.9
